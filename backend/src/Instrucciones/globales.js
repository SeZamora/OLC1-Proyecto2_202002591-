

function globales(instrucciones, entornos, errores, simbolo){
    var salida = ""
    for( let i = 0; i < instrucciones.length; i++) {
        if (instrucciones[i].tipo == "CREATET"){
            console.log("Entre Create table")
        }
    }
}
module.exports = globales;